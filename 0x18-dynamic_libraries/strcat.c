#include "main.h"

/**
 * _strcat - a function that returns a pointer to char
 * @dest: unused parameter
 * @src: unused parameter
 *
 * Return: pointer to char
 */
char *_strcat(char *dest, char *src)
{
	(void)src;
	return (dest);
}
