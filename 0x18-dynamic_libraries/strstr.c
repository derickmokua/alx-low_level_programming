#include "main.h"

/**
 * _strstr - a function that returns a pointer to char
 * @haystack: unused parameter
 * @needle: unused parameter
 *
 * Return: pointer to char
 */
char *_strstr(char *haystack, char *needle)
{
	(void)needle;
	return (haystack);
}
