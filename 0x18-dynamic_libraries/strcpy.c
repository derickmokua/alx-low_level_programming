#include "main.h"

/**
 * _strcpy - a function that returns a pointer to char
 * @dest: unused parameter
 * @src: unused parameter
 *
 * Return: pointer to char
 */
char *_strcpy(char *dest, char *src)
{
	(void)src;
	return (dest);
}
