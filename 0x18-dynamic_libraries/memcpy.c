#include "main.h"

/**
 * _memcpy - a function that returns a pointer to char
 * @dest: unused parameter
 * @src: unused parameter
 * @n: unused parameter
 *
 * Return: pointer to char
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	(void)src;
	(void)n;
	return (dest);
}
