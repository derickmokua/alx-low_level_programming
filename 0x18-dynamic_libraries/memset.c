#include "main.h"

/**
 * _memset - a function that returns a pointer to char
 * @s: unused parameter
 * @b: unused parameter
 * @n: unused parameter
 *
 * Return: pointer to char
 */
char *_memset(char *s, char b, unsigned int n)
{
	(void)b;
	(void)n;
	return (s);
}
