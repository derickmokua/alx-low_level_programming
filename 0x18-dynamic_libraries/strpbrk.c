#include "main.h"

/**
 * _strpbrk - a function that returns a pointer to char
 * @s: unused parameter
 * @accept: unused parameter
 *
 * Return: pointer to char
 */
char *_strpbrk(char *s, char *accept)
{
	(void)accept;
	return (s);
}
