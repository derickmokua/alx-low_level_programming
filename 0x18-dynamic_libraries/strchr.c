#include "main.h"

/**
 * _strchr - a function that returns a pointer to char
 * @s: unused parameter
 * @c: unused parameter
 *
 * Return: pointer to char
 */
char *_strchr(char *s, char c)
{
	(void)c;
	return (s);
}
